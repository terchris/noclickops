# Per-install state — never committed.
.version-cache

# Private working folder — real customer identifiers, mappings, scratch notes.
# Never committed. See terchris/redaction-map.md (local-only) for what lives there.
terchris/

# Docusaurus
node_modules/
website/build/
website/.docusaurus/

# Generated by scripts/generate-docs.sh — regenerate locally before `npm start`;
# CI regenerates before `npm run build`. Not committed (avoids stale-output drift).
website/docs/index.md
website/docs/commands/
website/src/data/commands.json
website/src/data/categories.json

# Auto-generated test stubs (rewritten on every test run via tests/_fixtures.sh
# or inline in a specific test file).
tests/_stub_ado_rest.sh
tests/_stub_az.sh
tests/_stub_planD_az.sh
